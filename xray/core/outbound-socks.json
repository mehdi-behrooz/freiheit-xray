{
    "outbounds": [
        {
            "tag": "proxy-socks",
            "protocol": "socks",
            "settings": {
                "servers": [
                    {
                        "address": "${PROXY_ADDRESS}",
                        "port": ${PROXY_PORT}
                    }
                ]
            },
            "streamSettings": {
                "network": "tcp",
                "security": "none"
            }
        }
    ]
}
