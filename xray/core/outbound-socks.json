{
    "outbounds": [
        {
            "tag": "warp-socks",
            "protocol": "socks",
            "settings": {
                "servers": [
                    {
                        "address": "${WARP_ADDRESS}",
                        "port": ${WARP_PORT}
                    }
                ]
            },
            "streamSettings": {
                "network": "tcp",
                "security": "none"
            }
        }
    ]
}
